{
  "name": "mini-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve . -l 5500",
    "test:jest": "jest",
    "test:cypress": "cypress run --headless --browser chrome"
  },
  "devDependencies": {
    "cypress": "^15.3.0",
    "jest": "^29.6.1",
    "serve": "^14.2.0"
  }
}
